# 投票分数修复说明

## 问题描述
投票时出现错误：
```
投票信息上链失败: 发送交易失败: 估算gas失败: execution reverted: Score must be between 1 and 10
```

## 问题原因
在 `backend/services/vote_service.go` 中，投票上链时传递的分数是 `100`，但智能合约要求分数必须在 **1 到 10** 之间。

### 合约限制
在 `HackathonEvent.sol` 合约的 `vote` 函数中有以下验证：
```solidity
require(_score >= 1 && _score <= 10, "Score must be between 1 and 10");
```

## 解决方案
已将投票分数从 `100` 修改为 `10`（满分）。

### 修改位置
文件：`backend/services/vote_service.go`
行号：第 70 行

### 修改前
```go
txHash, err := blockchainService.Vote(hackathon.ChainEventID, submissionID, 100)
```

### 修改后
```go
txHash, err := blockchainService.Vote(hackathon.ChainEventID, submissionID, 10)
```

## 重启服务
修改完成后，需要重新编译并重启后端服务：

```bash
cd backend
go build -o main .
./main
```

## 测试
1. 确保用户已签到
2. 尝试对作品投票
3. 应该成功上链，不再出现分数错误

## 未来改进建议
如果需要更灵活的评分系统，可以考虑：

### 方案1：前端传递分数
修改投票接口，允许用户选择 1-10 的分数：
```go
type VoteRequest struct {
    Score uint64 `json:"score" binding:"required,min=1,max=10"`
}
```

### 方案2：修改合约
如果需要更大的分数范围（如 0-100），可以修改合约中的验证逻辑：
```solidity
require(_score >= 0 && _score <= 100, "Score must be between 0 and 100");
```
然后重新部署合约。

## 当前状态
✅ 已修复
✅ 已编译
🔄 需要重启服务
